# 🏗️ Project Architecture Overview

Visual guide to the Smart Placement Activity Tracker architecture.

---

## 🌐 System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │           React Frontend (Port 3000)                     │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │ Router (React Router)                              │  │   │
│  │  │ ├─ /login                (Login Page)              │  │   │
│  │  │ ├─ /register             (Register Page)           │  │   │
│  │  │ ├─ / [protected]          (Dashboard)              │  │   │
│  │  │ ├─ /add-activity [prot]   (Add Activity)           │  │   │
│  │  │ ├─ /activities [prot]     (Activity History)       │  │   │
│  │  │ └─ /applications [prot]   (Company Tracker)        │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  │                        ▲                                    │   │
│  │                        │ Axios                             │   │
│  │                        │ (with JWT interceptor)           │   │
│  │                        ▼                                    │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │ State Management (AuthContext)                     │  │   │
│  │  │ • user (logged-in user)                            │  │   │
│  │  │ • token (JWT)                                      │  │   │
│  │  │ • login/logout/register functions                  │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  │                                                             │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │ Local Storage                                      │  │   │
│  │  │ • JWT Token (for persistence)                      │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ HTTPS
                            │
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND SERVER                                │
│                  (Node.js + Express)                            │
│                     (Port 5000)                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ HTTP Routes                                              │   │
│  │ ├─ POST   /api/auth/register                             │   │
│  │ ├─ POST   /api/auth/login                                │   │
│  │ ├─ GET    /api/auth/me                    [Protected]    │   │
│  │ ├─ POST   /api/activities                 [Protected]    │   │
│  │ ├─ GET    /api/activities                 [Protected]    │   │
│  │ ├─ GET    /api/activities/weekly          [Protected]    │   │
│  │ ├─ DELETE /api/activities/:id             [Protected]    │   │
│  │ ├─ POST   /api/applications               [Protected]    │   │
│  │ ├─ GET    /api/applications               [Protected]    │   │
│  │ ├─ PATCH  /api/applications/:id           [Protected]    │   │
│  │ ├─ DELETE /api/applications/:id           [Protected]    │   │
│  │ └─ GET    /api/analytics/summary          [Protected]    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                            ▲                                      │
│                            │                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Middleware                                               │   │
│  │ ├─ CORS                                                  │   │
│  │ ├─ JSON Parser                                           │   │
│  │ ├─ Auth Middleware (JWT verification)                   │   │
│  │ └─ Error Handler                                         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                            ▲                                      │
│                            │                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Controllers (Business Logic)                             │   │
│  │ ├─ authController                                        │   │
│  │ │  ├─ register()                                         │   │
│  │ │  ├─ login()                                            │   │
│  │ │  └─ getMe()                                            │   │
│  │ ├─ activityController                                    │   │
│  │ │  ├─ createActivity()                                   │   │
│  │ │  ├─ getActivities()                                    │   │
│  │ │  ├─ getWeeklyActivities()                              │   │
│  │ │  └─ deleteActivity()                                   │   │
│  │ ├─ applicationController                                 │   │
│  │ │  ├─ createApplication()                                │   │
│  │ │  ├─ getApplications()                                  │   │
│  │ │  ├─ updateApplication()                                │   │
│  │ │  └─ deleteApplication()                                │   │
│  │ └─ analyticsController                                   │   │
│  │    └─ getAnalyticsSummary()                              │   │
│  │       ├─ calculateStreak()                               │   │
│  │       ├─ calculateWeeklyEffort()                         │   │
│  │       ├─ calculateDistribution()                         │   │
│  │       ├─ calculateReadinessScore()                       │   │
│  │       └─ getWeakArea()                                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                            ▲                                      │
│                            │                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Mongoose Models (Database)                               │   │
│  │ ├─ User {name, email, password_hash}                     │   │
│  │ ├─ Activity {userId, date, type, duration, confidence}  │   │
│  │ └─ Application {userId, company, role, status}          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ MongoDB Protocol
                            │
┌─────────────────────────────────────────────────────────────────┐
│                     DATABASE SERVER                              │
│              (MongoDB / MongoDB Atlas)                          │
│                                                                   │
│  Collections:                                                    │
│  ├─ users (authentication & user data)                          │
│  ├─ activities (logged activities)                              │
│  └─ applications (job applications)                             │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────┐
│ User Registration / Login                   │
└────────────────────┬────────────────────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │ Email & Password      │
         │ Validation            │
         └────────┬──────────────┘
                  │
         ┌────────▼──────────┐
         │ Hash Password     │ (bcryptjs)
         │ (if register)     │
         └────────┬──────────┘
                  │
         ┌────────▼──────────┐
         │ Save/Find User    │
         │ in Database       │
         └────────┬──────────┘
                  │
         ┌────────▼──────────┐
         │ Create JWT Token  │
         │ (7 day expiry)    │
         └────────┬──────────┘
                  │
         ┌────────▼──────────┐
         │ Return to Client  │
         │ Token + User Data │
         └────────┬──────────┘
                  │
         ┌────────▼──────────┐
         │ Store in         │
         │ localStorage     │
         └──────────────────┘


Protected Request Flow:
┌──────────────────────┐
│ Request to API       │
└────────┬─────────────┘
         │
    ┌────▼─────┐
    │ Axios    │ Reads token from localStorage
    │ Intercep-│ Adds: "Authorization: Bearer <token>"
    │ tor      │
    └────┬─────┘
         │
    ┌────▼──────────────┐
    │ Express Receives  │
    │ Request           │
    └────┬──────────────┘
         │
    ┌────▼──────────────┐
    │ Auth Middleware   │
    │ ├─ Extract token  │
    │ ├─ Verify JWT     │
    │ └─ Extract userId │
    └────┬──────────────┘
         │
    ┌────▼──────────────┐
    │ Controller        │
    │ (with userId)     │
    │ Queries by userId │
    └────┬──────────────┘
         │
    ┌────▼──────────────┐
    │ Return User Data  │
    │ Only             │
    └──────────────────┘
```

---

## 📊 Analytics Engine Flow

```
User Activities in Database
        │
        ▼
┌──────────────────────┐
│ Fetch All Activities │ for logged-in user
│ Sorted by Date       │
└──────────┬───────────┘
           │
     ┌─────┴─────┬──────────┬──────────┐
     │           │          │          │
     ▼           ▼          ▼          ▼
 ┌────────┐ ┌─────────┐ ┌──────────┐ ┌──────────┐
 │ Streak │ │ Weekly  │ │Distribution│ Readiness│
 │Calc.   │ │ Effort  │ │Calculation │ Score    │
 │        │ │         │ │            │Calculation
 └───┬────┘ └────┬────┘ └──────┬─────┘ └────┬─────┘
     │           │             │            │
     │           │             │            │
    Counts      Sums        Calculates   Combines
    consecutive hours       percentages   all 4
    days with   in last     per type     components
    activity    7 days
                (→hours)


Readiness Score Calculation:
┌──────────────────────────────────────────────────┐
│ Total Score = 0                                  │
├──────────────────────────────────────────────────┤
│ + (30% × Streak Component)                       │
│   └─ (streak_days / 14) × 30                     │
├──────────────────────────────────────────────────┤
│ + (30% × Weekly Effort Component)                │
│   └─ (hours_logged / 10) × 30                    │
├──────────────────────────────────────────────────┤
│ + (20% × Balance Component)                      │
│   ├─ +5 points per activity type present        │
│   └─ Penalties if any type > 70%               │
├──────────────────────────────────────────────────┤
│ + (20% × Interview Practice Component)           │
│   └─ (interview_count / 5) × 20                 │
├──────────────────────────────────────────────────┤
│ = Final Score (max 100)                          │
└──────────────────────────────────────────────────┘

Color Coding:
┌──────────────┬─────────────┬──────────┐
│ 0-40         │ 40-70       │ 70-100   │
│ 🔴 Red       │ 🟡 Yellow   │ 🟢 Green │
│ Needs work   │ Good        │ Excellent│
└──────────────┴─────────────┴──────────┘
```

---

## 🧩 Component Hierarchy

```
<App>
  <AuthProvider>
    <Header />
    │
    ├─ Navigation
    ├─ User Info
    └─ Logout Button

    <Routes>
      │
      ├─ /login
      │  └─ <Login />
      │     └─ Form Input
      │
      ├─ /register
      │  └─ <Register />
      │     └─ Form Input
      │
      ├─ / (Protected)
      │  └─ <ProtectedRoute>
      │     └─ <Dashboard />
      │        ├─ <StatCard /> (4x)
      │        │  ├─ Streak
      │        │  ├─ Weekly Effort
      │        │  ├─ Total Activities
      │        │  └─ Readiness Score
      │        ├─ <DistributionChart />
      │        │  └─ Recharts PieChart
      │        └─ Analytics Summary
      │
      ├─ /add-activity (Protected)
      │  └─ <ProtectedRoute>
      │     └─ <AddActivity />
      │        └─ Form Inputs
      │
      ├─ /activities (Protected)
      │  └─ <ProtectedRoute>
      │     └─ <ActivityHistory />
      │        └─ Activity List
      │           ├─ Date
      │           ├─ Duration
      │           ├─ Confidence
      │           └─ Delete Button
      │
      └─ /applications (Protected)
         └─ <ProtectedRoute>
            └─ <Applications />
               ├─ Add Form
               └─ Application List
                  ├─ Company Name
                  ├─ Role
                  ├─ Status
                  ├─ Edit Button
                  └─ Delete Button
  </AuthProvider>
</App>
```

---

## 🔄 Data Flow Example: Add Activity

```
User fills form at http://localhost:3000/add-activity
        │
        ▼
┌────────────────────┐
│ Form Validation    │ (client-side)
│ ├─ Date present    │
│ ├─ Type selected   │
│ ├─ Duration > 0    │
│ └─ Confidence 1-5  │
└────────┬───────────┘
         │ ✓ Valid
         ▼
┌────────────────────┐
│ Axios POST         │
│ /api/activities    │
└────────┬───────────┘
         │
         ├─ Interceptor adds:
         │  Authorization: Bearer <JWT_TOKEN>
         │
         ▼
┌─────────────────────────────────┐
│ Express Receives POST            │
│ /api/activities                 │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Auth Middleware                  │
│ ├─ Verify JWT                    │
│ ├─ Extract userId                │
│ └─ Attach to req.userId          │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ activityController              │
│ .createActivity()               │
├─────────────────────────────────┤
│ ├─ Validate inputs              │
│ ├─ Create Activity object       │
│ ├─ Set userId (from middleware) │
│ └─ Save to MongoDB              │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ MongoDB Save                     │
│ insertOne(activity)              │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Return to Client                │
│ 201 Created                      │
│ {                               │
│   _id, userId, date, type,      │
│   duration, confidence, notes   │
│ }                               │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Frontend Updates                 │
│ ├─ Redirect to /activities      │
│ ├─ Fetch updated activities     │
│ ├─ Update state                 │
│ └─ Re-render ActivityHistory    │
└────────┬────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ User sees new activity in list  │
└─────────────────────────────────┘
```

---

## 🏃‍♂️ Page Load Sequence

```
1. App.js loads
   └─ AuthProvider initializes
      └─ Check localStorage for token
         ├─ If token exists → GET /auth/me
         │  └─ Load user data
         └─ If no token → user = null

2. User not logged in
   └─ Router redirects to /login

3. User logs in
   ├─ POST /auth/login
   ├─ Store token in localStorage
   ├─ Set user in AuthContext
   └─ Redirect to /

4. Dashboard loads
   ├─ ProtectedRoute checks auth
   ├─ GET /analytics/summary
   ├─ Display stats (streak, effort, etc)
   ├─ Render DistributionChart
   └─ Show Readiness Score

5. User navigates to /activities
   ├─ ProtectedRoute checks auth
   ├─ GET /activities
   ├─ Display activity list
   └─ Show delete buttons

6. User clicks Add Activity
   ├─ Navigate to /add-activity
   ├─ Show form
   └─ On submit → POST /activities

7. After save
   ├─ Redirect to /activities
   ├─ Fetch updated list
   └─ Display new activity
```

---

## 📈 Scalability Considerations

```
Current Architecture:
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  React SPA   │◄──►│ Express API  │◄──►│  MongoDB     │
│  (Single     │    │  (Single     │    │  (Single     │
│  Instance)   │    │  Instance)   │    │  Instance)   │
└──────────────┘    └──────────────┘    └──────────────┘

Future Scaling:
┌──────────────────────────────────────────────────────┐
│ CDN (Static Assets)                                  │
└──────────────────────────────────────────────────────┘
           ▲
           │
┌──────────────────────────────────────────────────────┐
│ Load Balancer                                        │
└──────────────────────────────────────────────────────┘
           ▲
      ┌────┴────┐
      ▼         ▼
┌─────────┐ ┌─────────┐    ┌──────────────┐
│API      │ │API      │◄──►│ Cache Layer  │
│Inst. 1  │ │Inst. 2  │    │ (Redis)      │
└─────────┘ └─────────┘    └──────────────┘
      │         │                ▲
      └────┬────┘                │
           ▼
      ┌──────────────┐
      │ MongoDB      │
      │ Replica Set  │
      └──────────────┘
```

---

## 🔒 Security Flow

```
Frontend                   Backend                  Database
   │                         │                         │
   │ POST /register          │                         │
   ├──────────────────────►  │                         │
   │  (email, password)      │                         │
   │                         │ Hash password           │
   │                         │ (bcryptjs)              │
   │                         │ └─ 10 salt rounds       │
   │                         │                         │
   │                         │ Save to DB              │
   │                         ├────────────────────────►│
   │                         │ (only hash stored)      │
   │                         │                         │
   │◄──────────────────────  │                         │
   │ JWT Token               │                         │
   │ (7 day expiry)          │                         │
   │                         │                         │
   │ Store in localStorage   │                         │
   │                         │                         │
   │ GET /analytics/summary  │                         │
   │ (with JWT header)       │                         │
   ├──────────────────────►  │                         │
   │                         │ Verify JWT              │
   │                         │ Extract userId          │
   │                         │                         │
   │                         │ Query by userId         │
   │                         ├────────────────────────►│
   │                         │ (only user's data)      │
   │                         │                         │
   │◄──────────────────────  │                         │
   │ User-specific data      │                         │
```

---

**Study this diagram to understand the complete flow!** 📚
